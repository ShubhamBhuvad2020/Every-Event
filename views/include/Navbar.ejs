<div id="header" class="">
  <nav class="navbar navbar-expand-lg bg-danger px-4">
    <div class="container-fluid">
      <i class="bi bi-subtract mx-2 text-light h2"></i>
      <a class="navbar-brand text-light h1" aria-disabled="true">EveryEntry</a>
      <% if(searchBar=='true') { %>
      <form class="d-flex mx-auto" role="search" id="search-form">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Search Event"
          aria-label="Search"
          id="EventTitle"
          autocomplete="off"
        />
        <!-- <button class="btn btn-outline-light" type="submit">Search</button> -->
      </form>
      <% } %>
      <div class="collapse navbar-collapse" id="navbarScroll">
        <ul
          class="navbar-nav navbar-nav-scroll ms-auto"
          style="--bs-scroll-height: 100px"
        >
          <% if(Home=='true') { %>
          <li class="nav-item">
            <a class="nav-link text-light" aria-current="page" href="/home"
              >Home</a
            >
          </li>
          <% } %> <% if(Events=='true') { %>
          <li class="nav-item">
            <a class="nav-link text-light" href="/events">Events</a>
          </li>
          <% } %> <% if(addEventBtn=='true') { %>
          <li>
            <a class="nav-link text-light mr-5" href="/addEvent"
              ><div class="btn btn-sm btn-outline-light">Add Events</div></a
            >
          </li>
          <% } %> <% if(logoutBtn=='true') { %>
          <li>
            <a href="/logout"
              ><button
                class="btn btn-outline-secondary border border-light my-2 btn-sm text-light"
              >
                <i class="bi bi-toggle-off px-3"></i> Logout
              </button></a
            >
          </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>
</div>
